import SwiftUI

struct SkillsView: View {
    var body: some View {
        Text("Skills — coming soon").foregroundStyle(.secondary)
    }
}

struct InstalledSkillsListView: View {
    var body: some View {
        Text("Installed Skills — coming soon").foregroundStyle(.secondary)
    }
}

struct SkillDetailView: View {
    var body: some View {
        Text("Skill Detail — coming soon").foregroundStyle(.secondary)
    }
}

struct SkillEditorSheet: View {
    var body: some View {
        Text("Skill Editor — coming soon").foregroundStyle(.secondary)
    }
}

struct UpdatesView: View {
    var body: some View {
        Text("Updates — coming soon").foregroundStyle(.secondary)
    }
}

struct HubBrowseView: View {
    var body: some View {
        Text("Hub — coming soon").foregroundStyle(.secondary)
    }
}
